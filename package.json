{
  "name": "ecommerce_gamer",
  "version": "1.0.0",
  "prisma": {
    "seed": "ts-node --project apps/backend/tsconfig.json -r tsconfig-paths/register apps/backend/src/seed.ts",
    "schema": "apps/backend/prisma/schema.prisma"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:seed": "npx prisma db seed",
    "postinstall": "npx prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "domain",
    "apps/*"
  ],
  "description": "",
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "jwt-decode": "^4.0.0"
  }
}
